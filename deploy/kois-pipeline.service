[Unit]
Description=KOIS pipeline run
Requires=docker.service
After=docker.service

[Service]
Type=oneshot
WorkingDirectory=/opt/kois
Environment=COMPOSE_PROJECT_DIR=/opt/kois
ExecStart=/usr/bin/flock -n /tmp/kois-pipeline.lock /usr/bin/docker compose --profile batch run --rm pipeline
